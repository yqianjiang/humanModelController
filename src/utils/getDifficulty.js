/**
 * 特定关节从默认初始姿态旋转到到 angles 的难度（能不能做到），以 0～1 表示，0表示不可能做到，1表示肯定能做到
 * @param {*} joint
 * @param {*} angles [number, number, number] 分别表示 x, y, z 轴的旋转量
 */
export function getDifficulty(joint, angles) { }
